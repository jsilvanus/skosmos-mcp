services:
  skosmos-mcp:
    build: .
    image: skosmos-mcp:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      MCP_HTTP_HOST: 0.0.0.0
      MCP_HTTP_PORT: 3000
    ports:
      - "3020:3000"
